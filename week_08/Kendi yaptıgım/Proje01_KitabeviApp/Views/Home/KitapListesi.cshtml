@model List<Kitap>
@{
    ViewData["Title"] = "Kitap Listesi";
}

<h1 class="display-6">Kitap Listesi</h1>
<hr>
<a asp-controller="Home" asp-action="KitapEkle" class="btn btn-outline-primary">Yeni Kitap</a>
<hr>
<table class="table table-sm table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Adı</th>
            <th>Basım Yılı</th>
            <th>Sayfa Sayısı</th>
            <th>Kategori</th>
            <th>Yazar</th>
            <th width=135>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var k in Model)
        {
            <tr>
                <td>@k.Id</td>
                <td><a asp-controller="Home" asp-action="KitapListesi" asp-route-id=@k.Id>@k.Ad</a></td>
                <td>@k.BasimYili</td>
                <td>@k.SayfaSayisi</td>
                <td>@k.Kategori.Ad</td>
                <td>@k.Yazar.Ad</td>
                <td>
                    <a class="btn btn-sm btn-warning me-2" asp-controller="Home" asp-action="KitapGuncelle" asp-route-id=@k.Id>Güncelle</a>
                    <a class="btn btn-sm btn-danger me-2" asp-controller="Home" asp-action="KitapSil" asp-route-id=@k.Id>Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>



@* <div class="container">
    <div class="row">
        @foreach (var kitap in Model)
        {
            <partial name="_Kitap" model="kitap" />
        }
    </div>
</div> *@