@using YemekSitesi.Entity.Concrete
@model List<Category>
@*FoodControllerdan aldıgım category ve food bilgierini gsöteriyorum*@
@*<ul class="list-group list-group-horizontal mt-2 border border-danger justify-content-center mb-2">
    <li class="list-group-item me-3">
        <a class="list-group-item-action"
           asp-controller="Food"
           asp-action="FoodList"
           asp-route-categoryurl="">Tümünü göster</a>
    </li>
    <li class="list-group-item ms-3">
        @foreach (var category in Model)
        {
            <a class="list-group-item-action ms-5"
           asp-controller="Food"
           asp-action="FoodList"
           asp-route-categoryurl="@category.Url">@category.Name</a>
        }
    </li>
    
</ul>
*@
 @*@(ViewBag.SelectedCategory ==null ? "active" : "")*@
@*@(ViewBag.SelectedCategory == category.Url ? "active" : "")*@

<style>
    .renk{
        background-color: #FBD148;
    }
</style>




@*<nav class="nav nav-pills flex-column flex-sm-row mb-3 renk">
    <a class="flex-sm-fill text-sm-center nav-link text-danger" 
    aria-current="page"
       asp-controller="Food"
       asp-action="FoodList"
       asp-route-categoryurl="">Tümünü göster</a>

    @foreach (var category in Model)
    {
        <a class="flex-sm-fill text-sm-center nav-link text-danger"
       asp-controller="Food"
       asp-action="FoodList"
       asp-route-categoryurl="@category.Url">@category.Name</a>
    }   

    <hr />
  
</nav>*@

@*<div class="d-flex justify-content-around renk" >
    
     @foreach (var category in Model)
    {
        <nav class="navbar navbar-expand  ">
            <div class="container-fluid">
               
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link text-danger" aria-current="page"  
                            asp-controller="Food"
                            asp-action="FoodList"
                            asp-route-categoryurl="@category.Url">@category.Name</a>
                        </li>

                     
                    </ul>
                </div>
            </div>
        </nav>

    }

   
</div>*@

<div class="row ">
    <div class="col-10 d-flex justify-content-around renk">

        @foreach (var category in Model)
        {
            <nav class="navbar navbar-expand  ">
                <div class="container-fluid">

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link text-danger" aria-current="page"
                               asp-controller="Food"
                               asp-action="FoodList"
                               asp-route-categoryurl="@category.Url">@category.Name</a>
                            </li>


                        </ul>
                    </div>
                </div>
            </nav>

        }
    </div>
    <div class="col-2 renk">
        @await Html.PartialAsync("_Search")
    </div>
</div>