@using ShoppingApp.Web.Models.Dtos
@model ProductDto
<div class="card mb-2">
    <div class="card-img p-2 text-center">
        <img src="/images/@Model.ImageUrl" width="75" />
     </div>
    <div class="card-body">
        <h5 class=" card-title text-center">@Model.Name</h5>
        <div class="text-muted text-center">@($"{Model.Price:C2}")</div> @*C0 yazmak para birmi olucagını söyler 0 yazmak ise virgülden sonra basamak alma der*@
    </div>

    <div class="card-footer bg-light d-flex justify-content-around">
        <a class="btn btn-primary btn-sm"
            asp-controller="Shop"
            asp-action="ProductDetails"
           asp-route-producturl=@Model.Url>İncele</a> @*burdaki url'i program.cs deki url den anlıyor*@

        <a class="btn btn-success btn-sm">Sepete Ekle</a>
    </div>
</div>
